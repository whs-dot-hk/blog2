# Install hugo
```sh
(
hugo_version=0.104.3
curl -L https://github.com/gohugoio/hugo/releases/download/v$hugo_version/hugo_${hugo_version}_linux-amd64.tar.gz | sudo tar -xzv -C /usr/local/bin hugo
)
```
